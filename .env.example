# Mapbox Static Maps API token (get from https://account.mapbox.com/access-tokens/)
# No PUBLIC_ prefix — only used at build time in Astro frontmatter
MAPBOX_TOKEN=

# GitHub Personal Access Token for fetching activity data at build time
# Generate at: https://github.com/settings/tokens?type=beta
# Permissions: Public Repositories (read-only), no extra scopes needed
GITHUB_TOKEN=

